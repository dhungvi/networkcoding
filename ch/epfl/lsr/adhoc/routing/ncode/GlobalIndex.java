package ch.epfl.lsr.adhoc.routing.ncode;

import java.util.*;

public class GlobalIndex {
	private static Random rnd = new Random(System.currentTimeMillis());
	public static int globalIndex=rnd.nextInt();
}
